import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular-estetic-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
